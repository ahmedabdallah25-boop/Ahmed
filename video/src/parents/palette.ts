// "Clarity in the Quran" — the house style of section 01 of the scene pack,
// which was itself calibrated off the source channel's own frames. These five
// colours and no others; the restraint is the channel's signature.
export const P = {
  cream: '#F4EFE2',   // warm parchment ground
  navy: '#22375C',    // all linework, shadow masses, body type
  ochre: '#D08A28',   // every light source — and the second thumbnail line
  terracotta: '#B5623F',
  sage: '#7C8560',
  bone: '#E8DFC8',
};

// The film is 16:9 long-form, not a Short. It runs 29:45, which is well past
// YouTube's 180s Shorts cap, so it publishes as long-form and needs a 1280x720
// thumbnail — see CLAUDE.md's mandatory thumbnail rule and section 05 of the pack.
export const W = 1920;
export const H = 1080;

export const FONT = 'Inter, "Liberation Sans", Arial, sans-serif';
export const NASKH = 'Amiri, serif';
